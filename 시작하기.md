# 친구를 위한 시작 가이드 🇰🇷

조선왕조실록·승정원일기·동문휘고 등 9개의 한국사 사료를 **자연어로 검색**할 수 있게 만든 도구예요. AI(Claude)에게 "정조가 건륭제 만수절에 보낸 사절단 알려줘" 같은 식으로 물어보면 원전을 찾아 정리해 줍니다.

이 가이드는 **컴퓨터·개발 경험이 거의 없는 분**도 따라올 수 있게 풀어 썼습니다. 막히면 인호한테 물어보세요.

---

## 0. 미리 알아두기

- 시간: 처음 한 번만 셋업에 **30분~1시간** 정도 걸려요. 그 다음부터는 검색만.
- 디스크: 다 받으면 **약 30~70GB**. 처음에는 작은 데이터셋만 받으세요.
- 컴퓨터: Windows / Mac 모두 가능. 4년 이상된 노트북이면 검색 속도가 좀 느릴 수 있지만 작동은 합니다.
- 그래픽카드(GPU): 있으면 빠르고 없어도 작동해요. NVIDIA GeForce 같은 게 있으면 검색이 1~2초, 없으면 30초 정도.

---

## 1. Hugging Face 계정 만들기

데이터를 받으려면 **Hugging Face**라는 사이트의 계정이 필요해요. 깃허브와 비슷한, AI 모델·데이터를 공유하는 사이트입니다.

### 단계
1. https://huggingface.co/join 으로 가서 이메일·비밀번호로 가입.
2. 가입 확인 메일이 오면 클릭해서 인증.
3. 로그인하면 우측 상단에 본인 프로필이 보입니다. **사용자명(username)**을 기억해 두세요. 예: `kim_history_lover`

### 인호한테 알려주기
**가입한 username을 인호한테 카톡으로 알려주세요** (예: "내 hf 아이디 `kim_history_lover` 야"). 인호가 본인 데이터셋에 collaborator로 초대해야 데이터를 받을 수 있어요. 초대받기 전에는 다음 단계로 가도 데이터 다운로드 시 "권한 없음" 오류가 납니다.

### 액세스 토큰 발급
초대를 받았다는 답을 받으면, 다음 단계로:
1. https://huggingface.co/settings/tokens 접속
2. **"Create new token"** 버튼 클릭
3. 이름: 아무거나 (예: `my-laptop`)
4. **Token type: Read** 또는 **Fine-grained** 선택. (Write는 불필요)
5. **"Create token"** 누르면 `hf_XXXXXX...`로 시작하는 긴 문자열이 나옵니다. **이걸 메모장에 복사해 두세요. 한 번만 보여주고 다시 못 봅니다.**

---

## 2. 컴퓨터에 도구 설치

### 2.1. Git (이미 있으면 건너뛰기)
- **Windows**: https://git-scm.com/download/win 에서 설치파일 받아 그냥 다음다음 누르며 설치.
- **Mac**: 터미널에서 `xcode-select --install` 또는 https://git-scm.com/download/mac 에서 받기.

설치 확인 — 터미널 (Windows는 PowerShell이나 Git Bash, Mac은 Terminal) 열고:
```bash
git --version
```
숫자가 나오면 OK.

### 2.2. uv (Python 환경 관리자)

uv는 Python 패키지를 자동으로 깔아주는 도구예요. 명령 한 줄로 설치합니다.

- **Windows** (PowerShell 열기 → 우클릭 → "관리자 권한으로 실행"):
  ```powershell
  powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
  ```
- **Mac/Linux**:
  ```bash
  curl -LsSf https://astral.sh/uv/install.sh | sh
  ```

설치 후 터미널을 닫았다 다시 열고 확인:
```bash
uv --version
```

---

## 3. 코드 받기

원하는 폴더(예: `Documents`)로 이동해서:
```bash
cd Documents
git clone https://github.com/simoninhochoi/kuksa-vectordb.git
cd kuksa-vectordb
```
이제 `kuksa-vectordb` 폴더 안에 모든 코드가 들어왔습니다.

### Python 환경 만들기
프로젝트 폴더에서:
```bash
uv sync
```

처음에는 PyTorch·BGE-M3 등 큰 패키지를 받느라 **5~15분** 걸려요. 인터넷 속도가 느리면 더 걸립니다. 끝까지 기다려 주세요.

> 💡 GPU 가속을 활용하려면 NVIDIA 드라이버가 최신이어야 합니다. GPU가 없거나 드라이버가 오래되면 자동으로 CPU 모드로 작동하니 그냥 진행하세요.

---

## 4. 데이터 받기

### 4.1. Hugging Face 로그인
```bash
uv run hf auth login
```
- "Token (input will not be visible):" 라고 뜨면 아까 메모장에 복사해 둔 `hf_XXXXX...` 토큰을 붙여넣고 Enter.
- "Add token as git credential? [Y/n]" 에 **Y** 입력.
- "Login successful." 메시지가 나오면 OK.

### 4.2. 작은 데이터셋부터 받아 보기

먼저 **동문휘고** (200MB, 빠르게 받기 좋음)로 잘 작동하는지 확인:
```bash
uv run python scripts/download_data.py dongmun_hwigo
```

### 4.3. 더 받고 싶은 데이터셋 (필요한 만큼만)

전체 목록 확인:
```bash
uv run python scripts/download_data.py --list
```

자주 쓸 만한 것:
- `goryeosa_jeolyo` (130MB) — 고려사절요
- `goryeosa` (350MB) — 고려사
- `kuksa_history` (3GB) — 한국사료총서 121권 (구한말 사료 풍부)
- `sillok` (4GB) — **조선왕조실록**
- `seungjeongwon` (12GB, 압축 풀면 30GB) — **승정원일기** ★ 가장 크고 무거움

다 받기:
```bash
uv run python scripts/download_data.py --all
```

> 💡 **승정원일기는 따로 추가 설정**이 필요합니다 (아래 6번 참조). 처음에는 그것 빼고 시도해 보세요.

---

## 5. Claude로 사용하기

이 도구는 **Claude**(앤트로픽 AI)와 연결해서 자연어로 검색합니다. Claude를 어떤 형태로 쓸지 두 가지 방법.

### 방법 A: Claude Desktop (가장 쉬움) ★ 추천

GUI 앱입니다. 채팅창에 "정조 시기 사대 외교 알려줘" 식으로 물어보면 됨.

1. https://claude.ai/download 에서 **Claude Desktop** 받기 (Mac/Windows). 무료 또는 Pro 구독 필요.
2. 앱 설치 후 로그인.
3. **MCP 서버 등록** — 이게 핵심:
   - 앱에서 **설정 → Developer → Edit Config** 또는 직접 파일 열기:
     - **Mac**: `~/Library/Application Support/Claude/claude_desktop_config.json`
     - **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`
   - 빈 파일이거나 `{}`만 있을 텐데, 다음 내용으로 교체 (★ 본인의 절대 경로로 수정):

```json
{
  "mcpServers": {
    "sillok": {
      "command": "uv",
      "args": [
        "run", "--directory",
        "C:/Users/<본인이름>/Documents/kuksa-vectordb",
        "python", "src/sillok_mcp_server.py"
      ],
      "env": {"PYTHONIOENCODING": "utf-8"}
    },
    "dongmun-hwigo": {
      "command": "uv",
      "args": [
        "run", "--directory",
        "C:/Users/<본인이름>/Documents/kuksa-vectordb",
        "python", "src/dh_mcp_server.py"
      ],
      "env": {"PYTHONIOENCODING": "utf-8"}
    }
  }
}
```

> Mac이면 경로가 `/Users/<본인이름>/Documents/kuksa-vectordb` 형식. 받은 데이터셋만 등록하면 됩니다 (받지 않은 건 빼세요).

전체 9개 서버 템플릿은 코드 폴더의 `.mcp.json` 파일을 참고하세요.

4. Claude Desktop 완전히 종료(Cmd+Q / 작업표시줄에서 종료) 후 다시 실행.
5. 채팅창 우하단에 **🔧 도구 아이콘**이 보이면 성공. 클릭해서 `search_sillok`, `search_dongmun_hwigo` 등이 나오면 OK.

이제 채팅창에:
```
정조가 건륭제 80세 만수절에 보낸 사절단을 동문휘고에서 찾아서 정리해줘
```
같은 식으로 물으면 Claude가 자동으로 검색·정리해 줍니다.

### 방법 B: Claude Code (터미널 기반, 개발자용)

1. https://claude.com/product/claude-code 또는 npm으로 설치:
   ```bash
   npm install -g @anthropic-ai/claude-code
   ```
2. 프로젝트 폴더에서:
   ```bash
   cd kuksa-vectordb
   claude
   ```
3. 코드 폴더의 `.mcp.json`이 자동 인식되어 9개 서버 모두 활성화됩니다.

---

## 6. 승정원일기는 별도 — Docker 필요

승정원일기는 200만 청크 규모(다른 것의 5배+)라 **Docker Qdrant 서버**가 필수예요. 처음 시작할 때는 빼고, 익숙해진 뒤 도전하세요.

### 6.1. Docker Desktop 설치
- https://www.docker.com/products/docker-desktop/ 에서 받아 설치.
- 처음 실행 시 회원가입(무료) 또는 건너뛰기 가능.

### 6.2. Qdrant 컨테이너 시작
프로젝트 폴더에서:
```bash
docker run -d \
  --name qdrant_sjw \
  -p 6333:6333 \
  -v "$(pwd)/data/승정원일기/qdrant_server:/qdrant/storage" \
  --memory=6g \
  --restart unless-stopped \
  qdrant/qdrant
```

> Windows PowerShell에서는 `$(pwd)` 대신 `${PWD}`로 쓰세요.

확인:
```bash
curl http://localhost:6333/collections/sjw_full
```
points 가 ~2,000,000 정도 나오면 정상.

### 6.3. Claude Desktop 설정에 추가
`claude_desktop_config.json`의 `mcpServers`에 항목 추가:
```json
"seungjeongwon": {
  "command": "uv",
  "args": [
    "run", "--directory",
    "C:/Users/<본인이름>/Documents/kuksa-vectordb",
    "python", "src/sjw_mcp_server.py"
  ],
  "env": {"PYTHONIOENCODING": "utf-8"}
}
```

---

## 7. 실제 사용 예시

Claude 채팅창에서:

```
조선왕조실록 정조 기사에서 대명의리(對明義理)에 대한 부분을 검색해서 정리해줘
```

```
동문휘고에서 건륭제 80세 만수절(1790년) 사절 파견 관련 기사를 찾아줘
```

```
승정원일기에서 정조 시기 청과의 사대 외교에 대한 기사를 5건 찾아 원문과 함께 보여줘
```

```
사료총서에서 송시열·만동묘 관련 기사를 찾아줘
```

Claude가 자동으로 적절한 MCP 도구를 골라 검색·재순위·요약합니다.

---

## 8. 자주 발생하는 문제

### "Token has not been saved" / 401 Unauthorized
→ HF 토큰 인증이 안 됐거나, 인호가 친구를 collaborator로 추가하지 않은 상태. 1번 단계 다시 확인.

### `uv sync`가 너무 느려요
→ 인터넷 속도 문제. 5~30분까지도 정상. 그래도 안 끝나면 회사·학교 방화벽이 PyPI를 막고 있을 수 있어요. 집 와이파이 시도.

### "CUDA not available"
→ GPU가 없거나 NVIDIA 드라이버가 오래된 경우. 자동 CPU 모드로 작동하니 무시. 검색이 좀 느려질 뿐.

### Claude Desktop에서 도구 아이콘이 안 보여요
→ 1) `claude_desktop_config.json` JSON 문법 오류, 2) 절대경로 오타, 3) Claude를 완전히 종료 안 함. 종료 후 재시작.
→ JSON 검사: https://jsonlint.com 에 붙여넣어 확인.

### "Module not found: FlagEmbedding"
→ `uv sync` 가 끝까지 안 됐거나 잘못된 venv. 프로젝트 폴더에서 다시:
```bash
uv sync --reinstall
```

### 메모리 부족
→ BGE-M3가 약 2GB, 검색 시 추가 모델·캐시 합쳐 4-6GB 메모리 사용. 16GB 미만 PC에서는 다른 무거운 앱 끄고 시도.

### 디스크가 가득 차요
→ 안 쓰는 데이터셋 폴더를 지우면 됩니다 (예: 승정원일기 안 쓸 거면 `data/승정원일기/` 폴더 통째 삭제). 이후 필요해지면 download_data.py로 다시 받기.

---

## 9. 더 알고 싶다면

- **검색 결과가 어떻게 정확해지나** — BGE-M3 dense+sparse 하이브리드 + reranker 3단 구성. 자세한 건 `README.md` 와 `CLAUDE.md`.
- **새 데이터셋 추가하고 싶다면** — `CLAUDE.md` 에 6단계 파이프라인 매뉴얼 있음.
- **버그·기능 요청** — 인호한테 카톡 또는 GitHub 이슈.

즐거운 한국사 검색 되세요! 🏯📜
